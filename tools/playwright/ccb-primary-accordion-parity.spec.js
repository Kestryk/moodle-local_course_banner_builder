const {test, expect} = require('@playwright/test');
const ensure = (condition, message) => { if (!condition) throw new Error(message); };
const environment = () => { const required = ['EASYEDU_MOODLE_URL', 'EASYEDU_MOODLE_USERNAME', 'EASYEDU_MOODLE_PASSWORD', 'EASYEDU_CCB_PRIMARY_ACCORDION_SOURCE_CATEGORY_ID']; const missing = required.filter(name => !process.env[name]); ensure(!missing.length, `Missing primary-accordion environment values: ${missing.join(', ')}`); const categoryId = String(process.env.EASYEDU_CCB_PRIMARY_ACCORDION_SOURCE_CATEGORY_ID); ensure(/^\d+$/.test(categoryId), 'Primary-accordion source category must be numeric.'); return {baseUrl: String(process.env.EASYEDU_MOODLE_URL).replace(/\/$/, ''), username: process.env.EASYEDU_MOODLE_USERNAME, password: process.env.EASYEDU_MOODLE_PASSWORD, categoryId}; };
const login = async(page, env) => { await page.goto(`${env.baseUrl}/login/index.php`, {waitUntil: 'domcontentloaded'}); await page.locator('#username').fill(env.username); await page.locator('#password').fill(env.password); await page.locator('#loginbtn').click({noWaitAfter: true}); await expect(page).not.toHaveURL(/\/login\//, {timeout: 30000}); };
const inspectAccordion = async accordion => accordion.evaluate(details => { const summary = details.querySelector(':scope > summary'); const chevron = summary?.querySelector(':scope > .local-course-banner-builder-primary-accordion-chevron'); const legacyChevron = summary?.querySelector('.local-course-banner-builder-collapse-icon'); const style = getComputedStyle(summary); const chevronStyle = chevron ? getComputedStyle(chevron) : null; return {isDetails: details.tagName === 'DETAILS', isOpen: details.open, hasSummary: summary?.tagName === 'SUMMARY', minHeight: style.minHeight, paddingTop: style.paddingTop, paddingRight: style.paddingRight, paddingBottom: style.paddingBottom, paddingLeft: style.paddingLeft, borderBottomWidth: style.borderBottomWidth, marginBottom: style.marginBottom, visiblePrimaryChevronCount: chevron && chevronStyle.display !== 'none' ? 1 : 0, primaryChevronFontSize: chevronStyle?.fontSize || null, primaryChevronTransform: chevronStyle?.transform || null, legacyChevronDisplay: legacyChevron ? getComputedStyle(legacyChevron).display : null}; });
test('CCB primary accordions keep desktop parity with Course Banner options', async({page}) => { const env = environment(); await login(page, env); await page.goto(`${env.baseUrl}/local/course_banner_builder/admin_manage.php?sourcecategoryid=${encodeURIComponent(env.categoryId)}`, {waitUntil: 'domcontentloaded'}); const selected = page.locator('details[data-primary-accordion="selected-source"]'); const configured = page.locator('details[data-primary-accordion="configured-sources"]'); const reference = page.locator('details.local-course-banner-builder-options-panel').first(); const settings = selected.locator(':scope > summary .local-course-banner-builder-settings-action'); await expect(selected).toHaveCount(1); await expect(configured).toHaveCount(1); await expect(reference).toHaveCount(1); await expect(settings).toHaveCount(1); await expect(selected).toHaveAttribute('open', 'open'); await expect(configured).toHaveAttribute('open', 'open'); await expect(settings).toHaveAttribute('type', 'button'); await expect(settings).toHaveAttribute('data-action', 'local-course-banner-builder-summary-action'); await expect(settings).toHaveAttribute('data-bs-target', '#local-course-banner-builder-source-settings-modal'); await reference.evaluate(details => { details.open = true; }); const [referenceSurface, selectedSurface, configuredSurface] = await Promise.all([inspectAccordion(reference), inspectAccordion(selected), inspectAccordion(configured)]); for (const surface of [selectedSurface, configuredSurface]) { expect(surface.isDetails).toBe(true); expect(surface.hasSummary).toBe(true); expect(surface.isOpen).toBe(true); expect(surface.visiblePrimaryChevronCount).toBe(1); expect(surface.legacyChevronDisplay).toBe('none'); expect(surface.minHeight).toBe(referenceSurface.minHeight); expect(surface.paddingTop).toBe(referenceSurface.paddingTop); expect(surface.paddingRight).toBe(referenceSurface.paddingRight); expect(surface.paddingBottom).toBe(referenceSurface.paddingBottom); expect(surface.paddingLeft).toBe(referenceSurface.paddingLeft); expect(surface.borderBottomWidth).toBe(referenceSurface.borderBottomWidth); expect(surface.marginBottom).toBe(referenceSurface.marginBottom); expect(surface.primaryChevronFontSize).toBe('12.48px'); expect(surface.primaryChevronTransform).not.toBe('none'); } });
