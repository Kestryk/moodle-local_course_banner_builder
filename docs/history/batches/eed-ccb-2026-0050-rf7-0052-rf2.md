# EED-CCB-2026-0050 RF7 / EED-CCB-2026-0052 RF2

## Scope

This source-only corrective lot keeps the previously accepted Layer infos
layout, fixed action-button geometry, horizontal width and compact gap, then
addresses two visual defects reported on the current CCB preview:

- unlocked Image Layer infos cells no longer receive the legacy generic tint;
  their rest, hover and focus surface matches the containing row exactly;
- the Course/Site Banner Manager Source Preview right-panel shell stretches to
  the preview canvas height, without changing the button height or public
  banner geometry.

Border and Overlay locked-row semantic tints remain owned by their existing
PHP style branches and are not removed.

## Files and contracts

- `classes/manager.php` removes only the generic fallback returned for unlocked
  Image override cells;
- `scss/components/_admin-controls.scss` stretches the right-panel shell;
- `scss/components/_admin-layout.scss` stretches the two-column preview grid;
- `styles.css` is rebuilt from the Sass source;
- `tools/test-ccb-layer-panel-rf-contract.ps1` records the focused static
  expectations;
- `docs/testing/ccb-ui-harmonisation.md` and `CHANGELOG.md` document the
  visible contract and validation boundary.

## Validation boundary

Executed in the dedicated worktree:

- official Dart Sass 1.89.2 build;
- focused Layer info / preview-panel contract: passed;
- `php -l classes/manager.php`: passed;
- `git diff --check`: passed.

No Moodle runtime, cache purge, fixture mutation, browser, screenshot or
managed preview was run by this source owner. Human review remains required on
the named CCB preview after integration.

## Handoff

The source owner must commit and push the clean branch. The integration owner
may then promote this commit together with the other named wave commits and
run the single approved preview gate. If the human still sees a different
Image-cell surface, report the exact row state (rest, hover or focus) and
whether it is Border, Overlay or Image so a new corrective RF can remain
scoped.
